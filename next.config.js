/** @type {import('next').NextConfig} */

const nextConfig = {
    reactStrictMode: true,
    // env: {
    //     NEXT_APP_STAGE: process.env.NEXT_PUBLIC_APP_STAGE,
    //     NEXT_PUBLIC_INFURA_ID: process.env.NEXT_PUBLIC_INFURA_ID
    // }
}

module.exports = nextConfig
